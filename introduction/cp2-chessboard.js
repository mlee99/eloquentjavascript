// Chessboard

let hashTag = " ";
let size = 8;

for (let i = 0; hashTag.length <= 64; i++) {
    // if (hashTag.length % 8 == 0) {
    //     hashTag = hashTag + "\n";
    // }
    // else if (hashTag.length % 2 == 0) {
    //     hashTag += "#";
    // }
    // else {
    //     hashTag += " ";
    // }
    
    // if (hashTag.length % 2 == 0) {
    //     hashTag += "#";
    // }
    // else if(hashTag.length == size) {
    //     hashTag += "\n";
    // }
    // else {
    //     hashTag += " ";
    // }
}

console.log(hashTag)